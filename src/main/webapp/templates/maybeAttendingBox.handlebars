<div id="maybeAttendingBox" class="panel panel-default yellow-box">
    <div class="panel-body">
        <span class="player-count">{{maybeAttendingList.length}}</span>&nbsp;VET IKKE
        <hr>
        {{#each maybeAttendingList}}
            {{username}}<br>
        {{/each}}
    </div>
</div>
