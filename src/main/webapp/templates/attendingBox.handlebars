<div id="attendingBox" class="panel panel-default green-box">
    <div class="panel-body">
        <span class="player-count">{{attendingList.length}}</span>&nbsp;KOMMER
        <hr>
        {{#each attendingList}}
            {{name}}<br>
        {{/each}}
    </div>
</div>
